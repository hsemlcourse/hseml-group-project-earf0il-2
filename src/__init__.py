"""Source package for SVHN digit classification project.

Submodules are imported lazily so that lightweight tooling (pytest, flake8)
does not have to load tensorflow/xgboost just to inspect the package.
Use ``from src.utils import ...`` etc. directly.
"""
